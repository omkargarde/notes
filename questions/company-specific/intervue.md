# prutech

- How do you extract only the duplicate values from a JavaScript array?
- What is the fundamental difference between useState and useEffect in React?
- When would you choose useReducer over useState?
- What problem does the virtual DOM solve, and how does it work at a high level?
- What are the three core rules you must follow when writing React function components?
- How do useMemo and useCallback differ in purpose and usage?
- Write a TypeScript function that capitalizes the
  first alphabetic character of any input string.
- Show the minimal boilerplate for making an HTTP POST request inside a React component.
- Outline a reusable pattern for client-side pagination in a React application.
- Compare Redux-Thunk and Redux-Saga in terms of execution model and use-cases.
- How is a Redux action different from a Redux action creator?
- Contrast a Redux reducer with a Redux selector in both intent and signature.
- What is an HTTP interceptor, and how would you implement one for Axios or fetch?
- Define “higher-order component” (HOC) and state the primary reason for using one.
