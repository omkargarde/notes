omkar garde introduced himself as an engineering graduate from RBC Bangalore who passed out in 2020 and has since worked as a front-end developer for four and a half years, specializing in creating various web pages, working on API integration, and improving page performance for e-commerce and government websites. When asked about the architecture of his last project, omkar garde explained it used a micro-frontend architecture where each module was divided into its own microservice, organized into sub-modules, routes, pages, and components, along with separate folders for utilities and libraries; however, when asked about handling a production issue from end to end, omkar garde clarified that this specific project was still in development, and the main issues he addressed involved page performance, specifically reducing the large size of components to smaller parts to improve interaction speed and rendering efficiency. The discussion concluded with NxtWave Team asking about the differences between event loop, microtask queue, and macro task, to which omkar garde started explaining the role of the event loop in enabling asynchronous tasks within JavaScript's synchronous execution environment.
omkar garde explained that the promise queue, which has the highest precedence, is assigned to the microtask queue, which has the second precedence, and that the event loop continuously monitors the call stack, pushing pending completed functions from the microtask queue back to the call stack if it is empty. NxtWave Team then asked omkar garde about a NodeJS application running for two days with constantly increasing memory, which omkar garde stated they were unsure about, and subsequently asked omkar garde to explain how inheritance works internally in JavaScript and what happens if an unhandled rejection occurs in Node 16, to which omkar garde speculated that the entire app crashes in Node 16. Finally, NxtWave Team requested omkar garde to design a high-traffic e-commerce order system with 1 million users and 100,000 orders, but omkar garde responded that their main experience is in front-end development and they lack sufficient experience with NodeJS and such system design.
Latest update
NxtWave Team asked omkar garde about how an index improves performance in SQL, to which omkar garde explained that indexes help in identifying specific rows, maintaining the locality of indexes, and searching for specific values. When questioned about experience with microservices, SFCC, or SFR, omkar garde confirmed they had no experience with those technologies and was primarily focused on Angular. NxtWave Team then inquired if omkar garde had ever led a cross-functional team, which omkar garde had not, although they had worked with designers, QAs, and backend engineers. Finally, omkar garde detailed their method for implementing lazy loading by checking if a component is needed for the initial page load and using React lazy if it is not, and for a production issue where a button was not working, they would check the console log for errors, network requests if a network call was involved, and would seek help if these initial checks provided no insight.